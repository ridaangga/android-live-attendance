pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        jcenter() //jika tidak ada ini, applandeo calendar not found
        maven { url 'https://jitpack.io' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() //jika tidak ada ini, applandeo calendar not found
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Live Attendance App"
include ':app'
